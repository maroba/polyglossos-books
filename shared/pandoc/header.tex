% Gemeinsamer LaTeX-Header für alle Bücher (xelatex).
% Schriften kommen aus book.yaml (mainfont); hier nur Feinschliff.

% Checkbox-Zeichen (in Übungen) fehlen in vielen Brotschriften → LaTeX-Symbole
\usepackage{amssymb}
\usepackage{newunicodechar}
\newunicodechar{☐}{\ensuremath{\square}}
\newunicodechar{☒}{\ensuremath{\boxtimes}}

% Etwas Luft in Tabellen (Vokabelboxen)
\renewcommand{\arraystretch}{1.3}

% Dialog-Blockquotes dezent statt eingerückt-kursiv
\usepackage{mdframed}
\renewenvironment{quote}
  {\begin{mdframed}[leftline=true, rightline=false, topline=false, bottomline=false,
    linewidth=2pt, linecolor=gray!50, innerleftmargin=1em, skipabove=0.8em]}
  {\end{mdframed}}
